FROM python:3.12.8-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /runner

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt \
    && addgroup --system botrunner \
    && adduser --system --ingroup botrunner --home /runner botrunner

COPY app ./app

RUN chown -R botrunner:botrunner /runner
USER botrunner

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8090"]
